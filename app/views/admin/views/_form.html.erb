<%= form_tag admin_refresh_view_path, :as => :form, :method => :post do%>
  <% MovieEpisode.all.each do |episode| %>
    Episode - <%= episode.title %>
    <%= check_box_tag "episodes[]", episode.id%>
    </br>
  <% end %>
  <%= submit_tag("Обновить") %>
<% end %>