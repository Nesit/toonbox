<%= render 'shared/about_us_submenu' %>

<div id="breadcrumbs">
  <div class="wrapper">
    <%= link_to t(:about_studio), about_us_path %>
    <span class="drop"></span>
    <%= link_to t(:jobs), jobs_path %>
  </div>
</div>

<div id="center" class="bg-vacansies">
  <div class="wrapper">
    <div class="content-block radius-10">
      <div class="vacansies-list">
	<% @jobs.each do |job| %>
	  <div class="vacansies-item radius-10">
	    <div class="name"><%= job.title %></div>
	    <div class="info"><%= job.short_description %></div>
	    <div class="more-detail"><%= link_to t(:more), job_path(job), class: :radius %></div>
	  </div>
	<% end %>
	<div class="clear"></div>
      </div>
    </div>
  </div>
</div>
